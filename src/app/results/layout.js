export const metadata = {
  title: 'Texas Localist',
  description: 'Find local gems in the Lone Star State.',
};

export const viewport = {
  width: "device-width",
  initialScale: 1,
  viewportFit: "cover",
  themeColor: "#fdf8ee",
};

export default function ResultsLayout({ children }) {
  return children;
}
